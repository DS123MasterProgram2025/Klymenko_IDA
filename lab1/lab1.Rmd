---
title: "Лабораторна робота 1. Створення основи типового DataScience проекту"
output: 
  html_notebook:
    toc: yes
    toc_float: true
---

### Виконання завдання

#### Створення графіку

1. Створюю графік для функції $$y(x)=b_ox+b_1+b_2x^2$$ базовими засобами R.

```{r}
# Задаємо параметри функції
b0 <- 2
b1 <- 3
b2 <- 1.57

# Задаємо область визначення

x <- seq(-1, 1, .1)
y <- b0 + b1 * x + b2 * x^2

plot(x, y,
     type = "l",
     col = "red",
     main = "Графік функції",
     xlab = "x",
     ylab = "y"
     )
points(x, y,
       col = "blue")
```

#### Робота з данними(запис/читання)

1. Зберігаю розраховані значення у датафрейм.

```{r}
df <- data.frame(x = x, y = y)
```

2. З використанням пакету `rio` записую дані в *csv* файл.
```{r}
library(rio) 
export(df, "data/data.csv")
```

3. Аналогічно імпортую дані із збереженого файлу в інший датафрейм.

```{r}
library(kableExtra)
dfNew <-  import("data/data.csv")
```


#### Візуалізація

1. Виконую візуалізацію у вигляді таблиці.

```{r}
knitr::kable(head(dfNew),
             caption = "Табл. 1. Фрагмент таблиці даних",
             format = "html") %>% 
  kable_styling(
    bootstrap_options = "striped", 
    full_width = FALSE            
  )
```

2. Виконую візуалізацію засобами ggplot2 із заданими параметрами фіункції.


```{r}
library(ggplot2)
library(dplyr)

b0 <- 2
b1 <- 3
b2 <- 1.57

# Формування даних та побудова графіка за допомогою потокового оператора %>%
plot_gg <- data.frame(x = seq(-1, 1, 0.1)) %>%
  mutate(y = b0 + b1 * x + b2 * x^2) %>%
  ggplot(aes(x = x, y = y)) +
  geom_line(color = "red") +

  geom_point(color = "blue") +

  labs(
    title = "Графік функції: y = 2 + 3x + 1.57x^2",
    x = "x",
    y = "y"
  ) +

  theme_minimal()

# Виведення графіка
plot_gg
```
